services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: yieldpilot
      POSTGRES_PASSWORD: yieldpilot
      POSTGRES_DB: yieldpilot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
